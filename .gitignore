# dependencias
node_modules

# salida del build (Vercel la genera con `npm run build`)
dist

# caché generada por Astro
.astro

# salida del adaptador de Vercel
.vercel

# variables de entorno — nunca versionar (.env.example sí se versiona)
.env
.env.*
!.env.example

# notas locales
Claude.md

.DS_Store
